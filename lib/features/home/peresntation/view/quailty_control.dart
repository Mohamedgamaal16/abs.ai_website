import 'package:flutter/material.dart';

class QuailtyControl extends StatelessWidget {
  const QuailtyControl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("quailty control");
  }
}
